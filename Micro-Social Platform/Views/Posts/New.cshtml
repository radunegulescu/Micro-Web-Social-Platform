@model Micro_Social_Platform.Models.Post
<div class="jumbotron">
    @if (Model.GroupId != null)
    {
        <h1>Group name: @ViewBag.gr.Name</h1>
        <br />
    }

    @using (Html.BeginForm(actionName: "New", controllerName: "Posts"))


    {
        @Html.Hidden("GroupId")
        @Html.Label("Title", "Title Post")
        <br />
        @Html.TextBox("Title", null, new { @class = "form-control" })
        @Html.ValidationMessage("Title", null, new { @class = "text-danger" })
        <br /><br />

        @Html.Label("Content", "Content Post")
        <br />
        @Html.TextArea("Content", null, new { @class = "form-control summernote" })
        @Html.ValidationMessage("Content", null, new { @class = "text-danger" })
        <br /><br />

        <button class="btn btn-sm button" type="submit">Submit</button>

    }

</div>
